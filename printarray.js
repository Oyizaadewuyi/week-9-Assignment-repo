//Build a simple loop, inside a function, that takes an array and prints the value of the array to the console.
//Solution

function printArrayValues(){
    const array=["jemi","basit","aaliyah","subur","hiba"];
    for (let i=0; i<array.length; i++);
    return array;
}
console.log(printArrayValues())